Attach this logo In Urdu & English card the sub heading is "Fluent in both languages, understanding cultural nuances seamlessly." But my PakBot can speak 19 local languages "Urdu, English, Punjabi, Sindhi, Pashto, Balochi, Saraiki, Kashmiri, Gujarati, Shina, Hindko, Brahui, Wakhi, Kalasha, Burushaski, Gujjari, Torwali, Kundal, Kohistan". Add API key copy option in Admin page. Also add PakBot Product of Faiwebz and add Faiwebz link faiwebz.com also Add image/vision support to this existing API. Here's exactly what to do:

Check what language and framework this API is built in first

Add a new field image (base64 string) to the existing chat endpoint — it should be optional, not required

When image is received along with text message, forward both to Claude Vision API or GPT-4 Vision API for understanding

When only text is received (no image), keep existing behavior exactly as it is

Return the vision response back in the same format as existing chat responses

Do NOT break any existing endpoints or logic

Do NOT change any existing code structure — only ADD vision support

ALSO Add URL reading support to this existing API. Here's exactly what to do:

Check what language and framework this API is built in first

Add a new field url (string) to the existing chat endpoint — it should be optional, not required

When URL is received, fetch the content of that URL and extract the main text from it

Pass that extracted text along with the user's message to the AI so it can respond based on the URL content

When no URL is provided, keep existing behavior exactly as it is

Handle errors gracefully — if URL is invalid or unreachable, return a friendly error message

Do NOT break any existing endpoints or logic

Do NOT change any existing code structure — only ADD URL reading support.
Add streaming support to this Express chat route:

Add a new /chat/stream POST route (keep old /chat route as is)

In the new stream route, replace openai.chat.completions.create with streaming version:

js
const completion = await openai.chat.completions.create({
  model,
  messages: [...],
  stream: true, // ADD THIS
});
Set these headers before streaming:
js
res.setHeader('Content-Type', 'text/event-stream');
res.setHeader('Cache-Control', 'no-cache');
res.setHeader('Connection', 'keep-alive');
Stream chunks to frontend like this:
js
for await (const chunk of completion) {
  const text = chunk.choices[0]?.delta?.content ?? '';
  if (text) res.write(text);
}
res.end();
Store full reply in history and update requestCount AFTER stream ends

Do NOT touch existing /chat route or any other logic!